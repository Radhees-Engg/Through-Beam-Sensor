# Through-Beam-Sensor
Simple laser–LDR through-beam counter. Reads the LDR, applies a threshold, and counts beam breaks. Removed a blocking sampling loop to keep it fast enough for quick objects. Source code for my demo.
